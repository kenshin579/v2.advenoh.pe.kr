# v2 포트폴리오 EN/KO 다국어 지원 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v2 포트폴리오 정적 사이트를 영어(루트 `/`)/한국어(`/ko/`) 2개 언어로 제공한다.

**Architecture:** markora.advenoh.pe.kr의 i18n 패턴을 채택한다 — `app/page.tsx`(en)와 `app/ko/page.tsx`(ko) 두 라우트가 공통 `loadHomeData(locale)`로 데이터를 로딩하고 `<ProfileShell locale t={dict} ... />`를 렌더한다. UI 문자열은 `lib/i18n/`의 `en`/`ko` 사전(타입으로 키 일치 강제)으로, 직접 작성 콘텐츠는 마크다운 frontmatter의 `_en`/`_ko` 접미사 필드로 관리한다. `output: 'export'`로 `out/index.html`(en) + `out/ko/index.html`(ko)가 생성된다.

**Tech Stack:** Next.js 16 App Router(static export), React 19, TypeScript, gray-matter, zod, Playwright.

**번역 정책 (중요):** 터미널/코드풍 토큰(`readme.md`, `projects/`, `writing.it`, `ls ./projects`, `cat README.md`, `$ quote-of-the-day`, `#02`, `//_`, `frank@seoul:~/profile`, 기호 `→ ← ↗ ↵ · — ★ ▸`)은 **양 언어 공통으로 두고 번역하지 않는다.** 사람이 읽는 라벨·aria-label·placeholder·섹션 제목만 사전으로 번역한다.

---

## File Structure

**신규**
- `lib/i18n/types.ts` — `Locale`, `Dict` 타입 재노출
- `lib/i18n/en.ts` — 영어 사전(원본, `export type Dict = typeof en`)
- `lib/i18n/ko.ts` — 한국어 사전(`Dict` 타입 강제)
- `lib/home-data.ts` — en/ko 공통 데이터 로딩 헬퍼
- `app/ko/layout.tsx` — ko 전용 metadata(canonical, hreflang)
- `app/ko/page.tsx` — ko 라우트
- `components/profile/LangToggle.tsx` — EN/KO 토글 pill
- `components/profile/AutoLangRedirect.tsx` — 브라우저 언어 감지 redirect

**수정**
- `lib/portfolio.ts` — `getPortfolioItems(locale)` + `_en`/`_ko` 필드
- `lib/profile-readme.ts` — `loadReadme(locale)` + `_en`/`_ko` 필드
- `lib/structured-data.ts` — locale 인자
- `app/layout.tsx` — 기본 `lang="en"`, OG locale, hreflang
- `app/page.tsx` — en 라우트로 정리 + `loadHomeData` + `AutoLangRedirect`
- `app/sitemap.ts` — en/ko URL + hreflang
- `components/profile/ProfileShell.tsx` — `locale`/`t` props 수신·전파 + html lang 동기화
- `components/profile/TitleBar.tsx` — `LangToggle` 삽입
- 사람이 읽는 텍스트가 있는 `components/profile/*.tsx` (아래 Task 9~13에서 그룹별 처리)
- `contents/website/*/index.md` 6개, `contents/profile/readme.md` — 영어 카피 추가

---

## Task 1: i18n 사전 인프라

**Files:**
- Create: `lib/i18n/en.ts`
- Create: `lib/i18n/ko.ts`
- Create: `lib/i18n/types.ts`

- [ ] **Step 1: 영어 사전 작성 `lib/i18n/en.ts`**

```ts
export const en = {
  a11y: {
    sidebar: 'Profile sidebar',
    activityPanel: 'Profile activity panel',
    statusBar: 'Status bar',
    keyboardShortcuts: 'Keyboard shortcuts',
    openNavigation: 'Open navigation',
    openActivityPanel: 'Open activity panel',
    closeNavigation: 'Close navigation',
    closeActivityPanel: 'Close activity panel',
    commandPalette: 'Command palette',
    openTweaks: 'Open tweaks panel',
    closeTweaks: 'Close tweaks',
    githubProfile: 'GitHub profile: kenshin579',
    linkedinProfile: 'LinkedIn profile: Frank Oh',
    instagramProfile: 'Instagram: frank.photosnap',
    quoteDetail: 'View this quote on inspire-me',
    langToggle: 'Switch language',
  },
  sidebar: {
    searchJump: 'Search & jump',
    workspace: 'Workspace',
    status: 'Status',
    offline: 'offline',
    links: 'Links',
  },
  rightRail: {
    commits: 'Commits',
    latestPosts: 'Latest posts',
    noPosts: 'no recent posts',
    system: 'System',
    services: 'services',
    uptime: 'uptime · 90d',
    region: 'region',
  },
  projects: {
    toNavigate: 'to navigate',
    featured: 'featured',
    open: 'open',
  },
  modal: {
    overview: 'Overview',
    stack: 'Stack',
    close: 'close · ESC',
    closeAria: 'Close',
    pressNavigate: 'to navigate projects',
    prev: 'prev',
    next: 'next',
    prevAria: 'Previous project',
    nextAria: 'Next project',
    openLive: 'Open live',
    metaStatus: 'status',
    metaYear: 'year',
    metaRole: 'role',
    metaUrl: 'url',
  },
  writing: {
    recent: '/ recent',
    noPosts: 'no recent posts',
  },
  palette: {
    label: 'Command Menu',
    placeholder: 'jump, search, open…',
    noResults: 'No results.',
    sections: 'Sections',
    projects: 'Projects',
    latestPosts: 'Latest posts',
    links: 'Links',
  },
  statusBar: {
    nav: 'nav',
    open: 'open',
    search: 'search',
    close: 'close',
  },
  tweaks: {
    title: 'tweaks',
    accent: 'accent',
    density: 'density',
    comfortable: 'comfortable',
    compact: 'compact',
    noise: 'noise',
  },
  stats: {
    servicesUp: 'services up',
    blogPosts: 'blog posts',
  },
  commits: {
    less: 'Less',
    more: 'More',
  },
  mobile: {
    navigation: 'Navigation',
    navigationDesc: 'Workspace sections, service status, and social links',
    activityPanel: 'Activity panel',
    activityPanelDesc: 'GitHub contributions, latest posts, and system metrics',
  },
  titleBar: {
    up: 'up',
  },
  meta: {
    title: 'Frank Oh — Backend / AI Engineer',
    description:
      'Frank Oh — Backend / AI Engineer in Seoul. Backend, distributed systems, AI, and Kubernetes GitOps infrastructure.',
  },
} as const

export type Dict = typeof en
```

- [ ] **Step 2: 한국어 사전 작성 `lib/i18n/ko.ts`**

```ts
import type { Dict } from './en'

export const ko: Dict = {
  a11y: {
    sidebar: '프로필 사이드바',
    activityPanel: '프로필 활동 패널',
    statusBar: '상태 표시줄',
    keyboardShortcuts: '키보드 단축키',
    openNavigation: '내비게이션 열기',
    openActivityPanel: '활동 패널 열기',
    closeNavigation: '내비게이션 닫기',
    closeActivityPanel: '활동 패널 닫기',
    commandPalette: '커맨드 팔레트',
    openTweaks: '설정 패널 열기',
    closeTweaks: '설정 닫기',
    githubProfile: 'GitHub 프로필: kenshin579',
    linkedinProfile: 'LinkedIn 프로필: Frank Oh',
    instagramProfile: 'Instagram: frank.photosnap',
    quoteDetail: 'inspire-me에서 이 명언 자세히 보기',
    langToggle: '언어 전환',
  },
  sidebar: {
    searchJump: '검색 & 이동',
    workspace: '작업 공간',
    status: '상태',
    offline: '오프라인',
    links: '링크',
  },
  rightRail: {
    commits: '커밋',
    latestPosts: '최근 글',
    noPosts: '최근 글 없음',
    system: '시스템',
    services: '서비스',
    uptime: '가동률 · 90일',
    region: '지역',
  },
  projects: {
    toNavigate: '이동',
    featured: '대표',
    open: '열기',
  },
  modal: {
    overview: '개요',
    stack: '기술 스택',
    close: '닫기 · ESC',
    closeAria: '닫기',
    pressNavigate: '프로젝트 이동',
    prev: '이전',
    next: '다음',
    prevAria: '이전 프로젝트',
    nextAria: '다음 프로젝트',
    openLive: '사이트 열기',
    metaStatus: '상태',
    metaYear: '연도',
    metaRole: '역할',
    metaUrl: '주소',
  },
  writing: {
    recent: '/ 최근',
    noPosts: '최근 글 없음',
  },
  palette: {
    label: '커맨드 메뉴',
    placeholder: '이동, 검색, 열기…',
    noResults: '결과 없음.',
    sections: '섹션',
    projects: '프로젝트',
    latestPosts: '최근 글',
    links: '링크',
  },
  statusBar: {
    nav: '이동',
    open: '열기',
    search: '검색',
    close: '닫기',
  },
  tweaks: {
    title: '설정',
    accent: '강조색',
    density: '밀도',
    comfortable: '여유',
    compact: '조밀',
    noise: '노이즈',
  },
  stats: {
    servicesUp: '서비스 정상',
    blogPosts: '블로그 글',
  },
  commits: {
    less: '적음',
    more: '많음',
  },
  mobile: {
    navigation: '내비게이션',
    navigationDesc: '작업 공간 섹션, 서비스 상태, 소셜 링크',
    activityPanel: '활동 패널',
    activityPanelDesc: 'GitHub 기여, 최근 글, 시스템 지표',
  },
  titleBar: {
    up: '정상',
  },
  meta: {
    title: 'Frank Oh — 백엔드 / AI 엔지니어',
    description:
      '서울의 백엔드 / AI 엔지니어 Frank Oh. 백엔드, 분산 시스템, AI, 쿠버네티스 GitOps 인프라.',
  },
}
```

- [ ] **Step 3: 타입 재노출 `lib/i18n/types.ts`**

```ts
export type { Dict } from './en'
export type Locale = 'en' | 'ko'
```

- [ ] **Step 4: 타입 정합성 검증**

Run: `npm run check`
Expected: 에러 없이 통과. (`ko`가 `Dict` 키를 하나라도 빠뜨리면 여기서 컴파일 에러로 잡힌다.)

- [ ] **Step 5: Commit**

```bash
git add lib/i18n/
git commit -m "feat: i18n 사전(en/ko) 및 타입 추가"
```

---

## Task 2: 포트폴리오 로더 locale 지원

**Files:**
- Modify: `lib/portfolio.ts`
- Test: `tests/portfolio.spec.ts` (신규, Playwright 아닌 노드 단위 검증은 어렵 → 아래는 로더 동작을 보장하는 임시 fixture 기반 단위 검증 대신 타입+빌드로 검증)

> 이 프로젝트엔 단위 테스트 러너가 없고 Playwright만 있다. 로더는 빌드 시점 실행이므로 **타입 검사 + 실제 콘텐츠 빌드 통과**로 검증한다. (E2E 검증은 Task 16.)

- [ ] **Step 1: zod 스키마에 `_en`/`_ko` 필드 추가**

`lib/portfolio.ts`의 `portfolioItemSchema`를 아래로 교체한다. 기존 무접미사 필드는 fallback 용도로 optional 유지한다.

```ts
export const portfolioItemSchema = z.object({
  site: z.string().url(),
  title: z.string().optional(),
  cover: z.string().optional(),
  stack: z.array(z.string()).optional(),
  featured: z.boolean().optional(),
  ext: z.string().optional(),
  order: z.number().optional(),

  // 번역 대상 — 무접미사(레거시 fallback) + _en/_ko
  description: z.string().optional(),
  description_en: z.string().optional(),
  description_ko: z.string().optional(),
  dek: z.string().optional(),
  dek_en: z.string().optional(),
  dek_ko: z.string().optional(),
  overview: z.string().optional(),
  overview_en: z.string().optional(),
  overview_ko: z.string().optional(),
  status: z.string().optional(),
  status_en: z.string().optional(),
  status_ko: z.string().optional(),
  year: z.string().optional(),
  year_en: z.string().optional(),
  year_ko: z.string().optional(),
  role: z.string().optional(),
  role_en: z.string().optional(),
  role_ko: z.string().optional(),
})
```

- [ ] **Step 2: locale 선택 헬퍼 + `getPortfolioItems(locale)` 구현**

`lib/portfolio.ts` 상단에 `import type { Locale } from './i18n/types'`를 추가하고, `getPortfolioItems`를 아래로 교체한다.

```ts
function pick(
  data: Record<string, unknown>,
  base: string,
  locale: Locale,
): string | undefined {
  const localed = data[`${base}_${locale}`]
  if (typeof localed === 'string' && localed.trim()) return localed
  const fallback = data[base]
  if (typeof fallback === 'string' && fallback.trim()) return fallback
  const other = data[`${base}_${locale === 'en' ? 'ko' : 'en'}`]
  return typeof other === 'string' ? other : undefined
}

export function getPortfolioItems(locale: Locale): PortfolioItem[] {
  const contentsDir = path.join(process.cwd(), 'contents/website')
  if (!fs.existsSync(contentsDir)) {
    console.warn('Portfolio contents directory not found:', contentsDir)
    return []
  }

  const entries = fs.readdirSync(contentsDir, { withFileTypes: true })
  const folders = entries.filter(entry => entry.isDirectory())

  const items = folders
    .map(folder => {
      const indexPath = path.join(contentsDir, folder.name, 'index.md')
      if (!fs.existsSync(indexPath)) {
        console.warn(`No index.md found in ${folder.name}`)
        return null
      }

      const fileContents = fs.readFileSync(indexPath, 'utf8')
      const { data } = matter(fileContents)
      const validated = portfolioItemSchema.parse(data)

      const description = pick(data, 'description', locale) ?? ''
      const dek = pick(data, 'dek', locale)
      const overview = pick(data, 'overview', locale)
      const title = validated.title || extractTitleFromUrl(validated.site)
      const overviewHtml = renderOverviewHtml(overview, folder.name)

      const item: PortfolioItem = {
        site: validated.site,
        title,
        description,
        cover: validated.cover,
        slug: folder.name,
        status: pick(data, 'status', locale),
        year: pick(data, 'year', locale),
        role: pick(data, 'role', locale),
        stack: validated.stack,
        dek,
        overview,
        featured: validated.featured,
        ext: validated.ext,
        order: validated.order,
        ...(overviewHtml ? { overviewHtml } : {}),
      }
      return item
    })
    .filter((item): item is PortfolioItem => item !== null)

  return items.sort((a, b) => {
    const aOrder = a.order ?? Number.POSITIVE_INFINITY
    const bOrder = b.order ?? Number.POSITIVE_INFINITY
    if (aOrder !== bOrder) return aOrder - bOrder
    return a.slug.localeCompare(b.slug)
  })
}
```

- [ ] **Step 3: 타입 검사**

Run: `npm run check`
Expected: 에러 없이 통과. (`getPortfolioItems` 호출부가 아직 인자를 안 넘겨 에러가 나면 Task 4에서 정리되므로, 이 시점엔 호출부도 함께 수정하거나 Task 4와 묶어 커밋한다. 우선 정의만 바꾼 뒤 다음 단계로.)

- [ ] **Step 4: Commit**

```bash
git add lib/portfolio.ts
git commit -m "feat: 포트폴리오 로더 locale별 필드 선택 지원"
```

---

## Task 3: 프로필 readme 로더 locale 지원

**Files:**
- Modify: `lib/profile-readme.ts`

- [ ] **Step 1: `loadReadme(locale)` 구현**

`lib/profile-readme.ts`를 아래로 교체한다. (본문은 frontmatter `body_en`/`body_ko`가 있으면 우선 사용하고, 없으면 기존 마크다운 본문을 fallback으로 쓴다.)

```ts
import fs from 'fs'
import path from 'path'
import matter from 'gray-matter'
import type { Locale } from './i18n/types'

export type ReadmeData = {
  role?: string
  focus?: string
  based?: string
  xp?: string
  headline?: string
  stack?: string[]
  db?: string[]
  cloud?: string[]
  cicd?: string[]
  body: string
}

function parseArray(v: unknown): string[] | undefined {
  if (Array.isArray(v)) {
    const arr = v.filter((x): x is string => typeof x === 'string').map(s => s.trim()).filter(Boolean)
    return arr.length > 0 ? arr : undefined
  }
  if (typeof v === 'string') {
    const arr = v.split(',').map(s => s.trim()).filter(Boolean)
    return arr.length > 0 ? arr : undefined
  }
  return undefined
}

function pickStr(data: Record<string, unknown>, base: string, locale: Locale): string | undefined {
  const localed = data[`${base}_${locale}`]
  if (typeof localed === 'string' && localed.trim()) return localed
  const fallback = data[base]
  if (typeof fallback === 'string' && fallback.trim()) return fallback
  const other = data[`${base}_${locale === 'en' ? 'ko' : 'en'}`]
  return typeof other === 'string' ? other : undefined
}

export function loadReadme(locale: Locale): ReadmeData {
  try {
    const p = path.join(process.cwd(), 'contents/profile/readme.md')
    const raw = fs.readFileSync(p, 'utf8')
    const { data, content } = matter(raw)
    const body = pickStr(data, 'body', locale) ?? content.trim()
    return {
      role: pickStr(data, 'role', locale),
      focus: pickStr(data, 'focus', locale),
      based: pickStr(data, 'based', locale),
      xp: pickStr(data, 'xp', locale),
      headline: pickStr(data, 'headline', locale),
      stack: parseArray(data.stack),
      db: parseArray(data.db),
      cloud: parseArray(data.cloud),
      cicd: parseArray(data.cicd),
      body,
    }
  } catch {
    return { body: '' }
  }
}
```

- [ ] **Step 2: 타입 검사**

Run: `npm run check`
Expected: 통과 (호출부는 Task 4에서 정리).

- [ ] **Step 3: Commit**

```bash
git add lib/profile-readme.ts
git commit -m "feat: 프로필 readme 로더 locale 지원"
```

---

## Task 4: 공통 데이터 로딩 헬퍼

**Files:**
- Create: `lib/home-data.ts`
- Modify: `lib/structured-data.ts`

- [ ] **Step 1: `lib/structured-data.ts`에 locale 인자 추가**

`getPersonStructuredData`와 `getWebSiteStructuredData`가 locale별 description을 받도록 시그니처를 바꾼다.

```ts
import { siteConfig } from './site-config'
import type { PortfolioItem } from './portfolio'

export function getPersonStructuredData(description: string) {
  return {
    '@context': 'https://schema.org',
    '@type': 'Person',
    name: siteConfig.author.name,
    url: siteConfig.url,
    sameAs: [
      siteConfig.author.social.linkedin,
      siteConfig.author.social.github,
      siteConfig.author.social.instagram,
    ],
    jobTitle: 'Software Engineer',
    description,
  }
}

export function getWebSiteStructuredData(description: string) {
  return {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: siteConfig.name,
    url: siteConfig.url,
    description,
    author: {
      '@type': 'Person',
      name: siteConfig.author.name,
    },
  }
}
```

(`getProjectsItemListStructuredData`는 그대로 둔다.)

- [ ] **Step 2: `lib/home-data.ts` 작성**

```ts
import { getPortfolioItems } from '@/lib/portfolio'
import { getStatusSnapshot } from '@/lib/status'
import { getGithubContrib } from '@/lib/github'
import { getWritingSections, getLatestPosts } from '@/lib/writing'
import { loadReadme } from '@/lib/profile-readme'
import type { Locale } from '@/lib/i18n/types'

export async function loadHomeData(locale: Locale) {
  const [portfolioItems, status, github, writingSections, latestPosts] =
    await Promise.all([
      Promise.resolve(getPortfolioItems(locale)),
      getStatusSnapshot(),
      getGithubContrib(),
      getWritingSections(),
      getLatestPosts(10),
    ])

  const readme = loadReadme(locale)

  return {
    portfolioItems,
    status,
    github,
    writing: {
      it: writingSections.it,
      investment: writingSections.investment,
      latest: latestPosts,
    },
    readme,
  }
}
```

- [ ] **Step 3: 타입 검사**

Run: `npm run check`
Expected: 통과.

- [ ] **Step 4: Commit**

```bash
git add lib/home-data.ts lib/structured-data.ts
git commit -m "feat: 공통 홈 데이터 로딩 헬퍼 및 구조화 데이터 locale 인자"
```

---

## Task 5: ProfileShell에 locale/t 전파 + html lang 동기화

**Files:**
- Modify: `components/profile/ProfileShell.tsx`

> 이 Task는 props 배선만 한다. 자식 컴포넌트의 실제 문자열 치환은 Task 9~13에서 한다. 지금은 `locale`/`t`를 받아 `useEffect`로 `document.documentElement.lang`을 동기화하고, 이미 `t`/`lang`을 받도록 바뀔 자식들에게 넘길 자리를 만든다.

- [ ] **Step 1: import 및 props 타입 추가**

`components/profile/ProfileShell.tsx` 상단 import에 추가:

```ts
import { useRef, useEffect } from 'react'
import type { Dict, Locale } from '@/lib/i18n/types'
```

`ProfileShellProps`에 두 필드 추가:

```ts
type ProfileShellProps = {
  locale: Locale
  t: Dict
  initialStatus: StatusSnapshot
  initialWriting: {
    it: WritingItem[]
    investment: WritingItem[]
    latest: WritingItem[]
  }
  portfolioItems: PortfolioItem[]
  github: GithubContrib
  readme: ReadmeData
}
```

함수 시그니처도 `locale`, `t`를 구조분해에 추가한다.

- [ ] **Step 2: html lang 동기화 effect 추가**

`useKeyboardNav(...)` 호출 아래에 추가:

```ts
useEffect(() => {
  document.documentElement.lang = locale
}, [locale])
```

- [ ] **Step 3: 자식에 `t`/`lang` 전달**

다음 컴포넌트 호출에 `t={t}`(및 필요한 곳은 `lang={locale}`)를 추가한다. 각 자식이 prop을 받도록 바뀌는 것은 Task 9~13이므로, **이 Step은 Task 9~13 직전까지 타입 에러가 날 수 있다.** 따라서 Step 3은 Task 9 시작 시점에 자식 시그니처 변경과 함께 진행한다(아래 Task 9 Step 0 참조). 지금은 Step 1~2만 적용하고 커밋한다.

- [ ] **Step 4: 타입 검사**

Run: `npm run check`
Expected: 통과 (아직 자식엔 prop 안 넘김).

- [ ] **Step 5: Commit**

```bash
git add components/profile/ProfileShell.tsx
git commit -m "feat: ProfileShell에 locale/t props 및 html lang 동기화"
```

---

## Task 6: 라우팅 — en 루트 정리 + ko 라우트 생성

**Files:**
- Modify: `app/page.tsx`
- Modify: `app/layout.tsx`
- Create: `app/ko/page.tsx`
- Create: `app/ko/layout.tsx`

- [ ] **Step 1: `app/page.tsx`를 en 라우트로 교체**

```tsx
import { loadHomeData } from '@/lib/home-data'
import { getProjectsItemListStructuredData } from '@/lib/structured-data'
import { ProfileShell } from '@/components/profile/ProfileShell'
import { AutoLangRedirect } from '@/components/profile/AutoLangRedirect'
import { en } from '@/lib/i18n/en'

export default async function HomePage() {
  const data = await loadHomeData('en')
  const itemList = getProjectsItemListStructuredData(data.portfolioItems)

  return (
    <>
      <AutoLangRedirect />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(itemList) }}
      />
      <ProfileShell
        locale="en"
        t={en}
        initialStatus={data.status}
        initialWriting={data.writing}
        portfolioItems={data.portfolioItems}
        github={data.github}
        readme={data.readme}
      />
    </>
  )
}
```

- [ ] **Step 2: `app/ko/page.tsx` 작성**

```tsx
import { loadHomeData } from '@/lib/home-data'
import { getProjectsItemListStructuredData } from '@/lib/structured-data'
import { ProfileShell } from '@/components/profile/ProfileShell'
import { ko } from '@/lib/i18n/ko'

export default async function KoHomePage() {
  const data = await loadHomeData('ko')
  const itemList = getProjectsItemListStructuredData(data.portfolioItems)

  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(itemList) }}
      />
      <ProfileShell
        locale="ko"
        t={ko}
        initialStatus={data.status}
        initialWriting={data.writing}
        portfolioItems={data.portfolioItems}
        github={data.github}
        readme={data.readme}
      />
    </>
  )
}
```

- [ ] **Step 3: `app/ko/layout.tsx` 작성**

```tsx
import type { Metadata } from 'next'
import { ko } from '@/lib/i18n/ko'

export const metadata: Metadata = {
  title: ko.meta.title,
  description: ko.meta.description,
  openGraph: {
    locale: 'ko_KR',
    title: ko.meta.title,
    description: ko.meta.description,
  },
  alternates: {
    canonical: '/ko/',
    languages: {
      en: '/',
      ko: '/ko/',
      'x-default': '/',
    },
  },
}

export default function KoLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
```

- [ ] **Step 4: `app/layout.tsx`의 root metadata/lang 갱신**

`app/layout.tsx`에서 `import { en } from '@/lib/i18n/en'`을 추가하고, metadata의 `title`/`description`을 `en.meta.title`/`en.meta.description`로, `openGraph.locale`을 `'en_US'`로 바꾸고, `alternates`를 추가한다. structured-data 호출에 description 인자를 넘기고, `<html lang="ko">`를 `<html lang="en">`으로 바꾼다.

변경 지점:
```tsx
import { en } from '@/lib/i18n/en'
// ...
export const metadata: Metadata = {
  metadataBase: new URL('https://advenoh.pe.kr'),
  title: en.meta.title,
  description: en.meta.description,
  authors: [{ name: siteConfig.author.name }],
  keywords: ['portfolio', 'web development', 'Frank Oh', '포트폴리오', '웹 개발', 'backend', '서버', 'AI'],
  alternates: {
    canonical: '/',
    languages: { en: '/', ko: '/ko/', 'x-default': '/' },
  },
  openGraph: {
    type: 'website',
    locale: 'en_US',
    url: siteConfig.url,
    title: en.meta.title,
    description: en.meta.description,
    siteName: siteConfig.name,
    images: [{ url: '/og-image.png', width: 1200, height: 630, alt: 'Frank Oh Portfolio' }],
  },
  // twitter/robots/verification 그대로
}
```

`RootLayout` 본문:
```tsx
const personData = getPersonStructuredData(en.meta.description)
const websiteData = getWebSiteStructuredData(en.meta.description)
// ...
<html lang="en" className="dark" data-accent="violet" data-density="comfortable">
```

- [ ] **Step 5: 타입 검사 + 빌드**

Run: `npm run check && npm run build`
Expected: 통과하고 `out/index.html`과 `out/ko/index.html`이 생성된다.

확인: `ls out/index.html out/ko/index.html`
Expected: 두 파일 모두 존재.

- [ ] **Step 6: Commit**

```bash
git add app/page.tsx app/ko/ app/layout.tsx
git commit -m "feat: en 루트 + /ko 라우트 및 root metadata 다국어화"
```

---

## Task 7: 언어 토글 & 자동 전환

**Files:**
- Create: `components/profile/LangToggle.tsx`
- Create: `components/profile/AutoLangRedirect.tsx`
- Modify: `components/profile/TitleBar.tsx`

- [ ] **Step 1: `AutoLangRedirect.tsx` 작성**

```tsx
'use client'

import { useEffect } from 'react'
import { useRouter } from 'next/navigation'

export function AutoLangRedirect() {
  const router = useRouter()
  useEffect(() => {
    if (localStorage.getItem('v2-lang')) return
    if (navigator.language.toLowerCase().startsWith('ko')) {
      router.replace('/ko/')
    }
  }, [router])
  return null
}
```

- [ ] **Step 2: `LangToggle.tsx` 작성**

```tsx
'use client'

import { usePathname, useRouter } from 'next/navigation'

type LangToggleProps = { label: string }

export function LangToggle({ label }: LangToggleProps) {
  const pathname = usePathname() || '/'
  const router = useRouter()
  const isKo = pathname.startsWith('/ko')

  function go(to: 'en' | 'ko') {
    if (typeof window !== 'undefined') {
      localStorage.setItem('v2-lang', to)
    }
    router.push(to === 'ko' ? '/ko/' : '/')
  }

  return (
    <div
      role="group"
      aria-label={label}
      className="inline-flex items-center overflow-hidden rounded border border-profile-line-2 text-[10px] font-mono"
    >
      <button
        type="button"
        onClick={() => go('en')}
        className={`px-1.5 py-0.5 ${!isKo ? 'bg-profile-bg-3 text-profile-accent' : 'text-profile-muted hover:text-profile-fg'}`}
      >
        EN
      </button>
      <button
        type="button"
        onClick={() => go('ko')}
        className={`px-1.5 py-0.5 ${isKo ? 'bg-profile-bg-3 text-profile-accent' : 'text-profile-muted hover:text-profile-fg'}`}
      >
        KO
      </button>
    </div>
  )
}
```

- [ ] **Step 3: `TitleBar.tsx`에 토글 삽입**

`components/profile/TitleBar.tsx` 상단에 `import { LangToggle } from './LangToggle'`와 `import type { Dict } from '@/lib/i18n/types'`를 추가하고, `TitleBarProps`에 `t: Dict`를 추가한 뒤 시그니처 구조분해에 `t`를 더한다. 우측 `ml-auto` 그룹(`⌘K` 버튼 앞)에 토글을 삽입한다.

`<div className="ml-auto flex items-center gap-2 md:gap-4">` 바로 안, 모바일 activity 버튼 다음 줄에:
```tsx
<LangToggle label={t.a11y.langToggle} />
```

- [ ] **Step 4: ProfileShell에서 TitleBar에 `t` 전달**

`components/profile/ProfileShell.tsx`의 `<TitleBar status={status} activeSection={activeSection} />`를 다음으로 바꾼다:
```tsx
<TitleBar status={status} activeSection={activeSection} t={t} />
```

- [ ] **Step 5: 타입 검사 + 개발 서버 수동 확인**

Run: `npm run check`
Expected: 통과.

Run: `npm run dev` 후 브라우저에서 `http://localhost:3000` → TitleBar 우측에 EN/KO 토글 노출, KO 클릭 시 `/ko/`로 이동.

- [ ] **Step 6: Commit**

```bash
git add components/profile/LangToggle.tsx components/profile/AutoLangRedirect.tsx components/profile/TitleBar.tsx components/profile/ProfileShell.tsx
git commit -m "feat: TitleBar 언어 토글 및 브라우저 언어 자동 전환"
```

---

## Task 8~13 공통 절차 (문자열 치환)

각 컴포넌트는 다음 절차로 동일하게 처리한다:

1. 컴포넌트 상단에 `import type { Dict } from '@/lib/i18n/types'` 추가.
2. props 타입에 `t: Dict` 추가, 함수 구조분해에 `t` 추가.
3. 표(아래 각 Task) 의 "old → new"대로 사람이 읽는 텍스트를 `{t.…}`로 치환. **터미널/코드풍 토큰과 기호는 건드리지 않는다.**
4. `ProfileShell.tsx`에서 해당 자식 호출에 `t={t}` 전달(이미 `status` 등 받는 자리 옆에).
5. `npm run check` 통과 확인 후 그룹 단위 커밋.

> ProfileShell이 자식에 넘기는 데이터(`status`, `github`, `readme`, `items` 등)는 그대로 두고 `t={t}`만 추가한다. `Sidebar`/`RightRail`처럼 내부적으로 `SidebarContent`/`RightRailContent`에 위임하는 경우, 부모도 `t`를 받아 자식에 그대로 넘긴다.

---

## Task 8: Sidebar 그룹 문자열 치환

**Files:**
- Modify: `components/profile/Sidebar.tsx`
- Modify: `components/profile/SidebarContent.tsx`
- Modify: `components/profile/MobileSidebarDrawer.tsx`
- Modify: `components/profile/ProfileShell.tsx`

- [ ] **Step 1: `Sidebar.tsx`** — `t: Dict` 추가, aria-label 치환, `SidebarContent`에 `t` 전달.

| old | new |
|---|---|
| `aria-label="Profile sidebar"` (14행) | `aria-label={t.a11y.sidebar}` |

`<SidebarContent ... />` 호출에 `t={t}` 추가.

- [ ] **Step 2: `SidebarContent.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 51 | `Search & jump` | `{t.sidebar.searchJump}` |
| 57 | `Workspace` | `{t.sidebar.workspace}` |
| 83 | `Status` (in `Status · …`) | `{t.sidebar.status}` |
| 103 | `offline` | `{t.sidebar.offline}` |
| 110 | `Links` | `{t.sidebar.links}` |
| 19 | `GitHub profile: kenshin579` | `{t.a11y.githubProfile}` |
| 24 | `LinkedIn profile: Frank Oh` | `{t.a11y.linkedinProfile}` |
| 29 | `Instagram: frank.photosnap` | `{t.a11y.instagramProfile}` |

> `readme.md`, `projects/`, `writing.it`, `writing.inv`, `github/kenshin579`, `↗`, `/` 등 코드풍 라벨/기호는 **그대로 둔다.**

- [ ] **Step 3: `MobileSidebarDrawer.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 28 | `Navigation` (aria) | `{t.mobile.navigation}` |
| 31 | `Navigation` (Title) | `{t.mobile.navigation}` |
| 33 | `Workspace sections, service status, and social links` | `{t.mobile.navigationDesc}` |
| 36 | `Close navigation` | `{t.a11y.closeNavigation}` |

`<SidebarContent ... />` 내부 호출에도 `t={t}` 전달.

- [ ] **Step 4: `ProfileShell.tsx`** — `<Sidebar ... t={t} />`, `<MobileSidebarDrawer ... t={t} />` 전달 추가.

- [ ] **Step 5: 검사 + 커밋**

Run: `npm run check`
Expected: 통과.
```bash
git add components/profile/Sidebar.tsx components/profile/SidebarContent.tsx components/profile/MobileSidebarDrawer.tsx components/profile/ProfileShell.tsx
git commit -m "feat: Sidebar 그룹 다국어 문자열 적용"
```

---

## Task 9: RightRail 그룹 문자열 치환

**Files:**
- Modify: `components/profile/RightRail.tsx`
- Modify: `components/profile/RightRailContent.tsx`
- Modify: `components/profile/MobileRightRailDrawer.tsx`
- Modify: `components/profile/CommitGraph.tsx`
- Modify: `components/profile/ProfileShell.tsx`

- [ ] **Step 1: `RightRail.tsx`** — `t: Dict` 추가, `aria-label="Profile activity panel"`(17행) → `{t.a11y.activityPanel}`, `RightRailContent`에 `t` 전달.

- [ ] **Step 2: `RightRailContent.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 19 | `Commits` (in `Commits · 26w`) | `{t.rightRail.commits}` |
| 27 | `Latest posts` | `{t.rightRail.latestPosts}` |
| 31 | `no recent posts` | `{t.rightRail.noPosts}` |
| 59 | `System` | `{t.rightRail.system}` |
| 63 | `services` | `{t.rightRail.services}` |
| 69 | `uptime · 90d` | `{t.rightRail.uptime}` |
| 73 | `region` | `{t.rightRail.region}` |

> `26w`, `90d`, `seoul`(값)은 그대로 둔다.

- [ ] **Step 3: `CommitGraph.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 68 | `Less` | `{t.commits.less}` |
| 76 | `More` | `{t.commits.more}` |

(34/48행 동적 aria/title은 영어 유지해도 무방 — YAGNI, 변경하지 않는다.)

`RightRailContent`/`MobileRightRailDrawer`에서 `<CommitGraph ... />` 호출에 `t={t}` 추가.

- [ ] **Step 4: `MobileRightRailDrawer.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 35 | `Activity panel` (aria) | `{t.mobile.activityPanel}` |
| 38 | `Activity panel` (Title) | `{t.mobile.activityPanel}` |
| 40 | `GitHub contributions, latest posts, and system metrics` | `{t.mobile.activityPanelDesc}` |
| 43 | `Close activity panel` | `{t.a11y.closeActivityPanel}` |

`<RightRailContent ... />`에 `t={t}` 전달.

- [ ] **Step 5: `ProfileShell.tsx`** — `<RightRail ... t={t} />`, `<MobileRightRailDrawer ... t={t} />` 전달 추가.

- [ ] **Step 6: 검사 + 커밋**

Run: `npm run check`
Expected: 통과.
```bash
git add components/profile/RightRail.tsx components/profile/RightRailContent.tsx components/profile/MobileRightRailDrawer.tsx components/profile/CommitGraph.tsx components/profile/ProfileShell.tsx
git commit -m "feat: RightRail 그룹 다국어 문자열 적용"
```

---

## Task 10: Projects 그룹 문자열 치환

**Files:**
- Modify: `components/profile/ProjectGrid.tsx`
- Modify: `components/profile/ProjectCardV2.tsx`
- Modify: `components/profile/ProjectModal.tsx`
- Modify: `components/profile/ProfileShell.tsx`

- [ ] **Step 1: `ProjectGrid.tsx`** — `t: Dict` 추가, 아래 치환. `ProjectCardV2`에 `t` 전달.

| 행 | old | new |
|---|---|---|
| 65-71 | `to navigate` | `{t.projects.toNavigate}` |

> `projects`, `//_`, `#02`, `ls ./projects · N items`, `j`, `k`는 코드풍이므로 그대로.

- [ ] **Step 2: `ProjectCardV2.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 57 | `featured` | `{t.projects.featured}` |
| 119, 197 | `open` (in `↵ open`) | `{t.projects.open}` |

> `★`, `▸`, `→`, `↵`, `Enter j k`(aria-keyshortcuts, 키 이름이므로) 그대로.

- [ ] **Step 3: `ProjectModal.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 99 | `Close` (aria) | `{t.modal.closeAria}` |
| 102 | `close · ESC` | `{t.modal.close}` |
| 151-152 | `Overview` | `{t.modal.overview}` |
| 164-165 | `Stack` | `{t.modal.stack}` |
| 185-192 | `to navigate projects` | `{t.modal.pressNavigate}` |
| 202 | `Previous project` (aria) | `{t.modal.prevAria}` |
| 202 | `prev` | `{t.modal.prev}` |
| 207 | `Next project` (aria) | `{t.modal.nextAria}` |
| 211 | `next` | `{t.modal.next}` |
| 219 | `Open live` | `{t.modal.openLive}` |
| 246 | `status`/`year`/`role`/`url` (MetaCell 라벨) | `{t.modal.metaStatus}`/`{t.modal.metaYear}`/`{t.modal.metaRole}`/`{t.modal.metaUrl}` |

> `~/projects/`, `↗`, `—`, `press ← →` 의 기호 부분은 그대로.

- [ ] **Step 4: `ProfileShell.tsx`** — `<ProjectGrid ... t={t} />`, `<ProjectModal ... t={t} />` 전달 추가.

- [ ] **Step 5: 검사 + 커밋**

Run: `npm run check`
Expected: 통과.
```bash
git add components/profile/ProjectGrid.tsx components/profile/ProjectCardV2.tsx components/profile/ProjectModal.tsx components/profile/ProfileShell.tsx
git commit -m "feat: Projects 그룹 다국어 문자열 적용"
```

---

## Task 11: Writing / Quote 그룹 문자열 치환

**Files:**
- Modify: `components/profile/WritingList.tsx`
- Modify: `components/profile/QuoteBlock.tsx`
- Modify: `components/profile/ProfileShell.tsx`

- [ ] **Step 1: `WritingList.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 32 | `/ recent` | `{t.writing.recent}` |
| 37 | `no recent posts` | `{t.writing.noPosts}` |

> `//_`, `title`(props로 들어오는 `writing.it`/`writing.inv`)은 그대로.

- [ ] **Step 2: `QuoteBlock.tsx`** — `t: Dict` 추가(현재 props 없음 → `{ t }: { t: Dict }`), 아래 치환.

| 행 | old | new |
|---|---|---|
| 99 | `inspire-me에서 이 명언 자세히 보기` (aria) | `{t.a11y.quoteDetail}` |

> `$ quote-of-the-day`는 코드풍이므로 그대로.

- [ ] **Step 3: `ProfileShell.tsx`** — `<QuoteBlock />` → `<QuoteBlock t={t} />`, `<WritingList ... t={t} />` 두 곳 모두 전달 추가.

- [ ] **Step 4: 검사 + 커밋**

Run: `npm run check`
Expected: 통과.
```bash
git add components/profile/WritingList.tsx components/profile/QuoteBlock.tsx components/profile/ProfileShell.tsx
git commit -m "feat: Writing/Quote 다국어 문자열 적용"
```

---

## Task 12: CommandPalette / StatusBar / Tweaks 문자열 치환

**Files:**
- Modify: `components/profile/CommandPalette.tsx`
- Modify: `components/profile/StatusBar.tsx`
- Modify: `components/profile/TweaksPanel.tsx`
- Modify: `components/profile/ProfileShell.tsx`

- [ ] **Step 1: `CommandPalette.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 77 | `Command palette` (aria) | `{t.a11y.commandPalette}` |
| 84 | `Command Menu` | `{t.palette.label}` |
| 90 | `jump, search, open…` | `{t.palette.placeholder}` |
| 98 | `No results.` | `{t.palette.noResults}` |
| 102 | `Sections` | `{t.palette.sections}` |
| 120 | `Projects` | `{t.palette.projects}` |
| 142 | `Latest posts` | `{t.palette.latestPosts}` |
| 168 | `Links` | `{t.palette.links}` |

> 16-19행의 `readme.md`/`projects/`/`writing.it`/`writing.inv`, `esc`, `#`, `—`, `github`, `status`는 코드풍/고유라 그대로.

- [ ] **Step 2: `StatusBar.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 9 | `Status bar` (aria) | `{t.a11y.statusBar}` |
| 19 | `Keyboard shortcuts` (aria) | `{t.a11y.keyboardShortcuts}` |
| 23 | `nav` | `{t.statusBar.nav}` |
| 26 | `open` | `{t.statusBar.open}` |
| 29 | `search` | `{t.statusBar.search}` |
| 32 | `close` | `{t.statusBar.close}` |

> `j/k`, `↵`, `⌘K`, `esc`, `·`는 그대로.

- [ ] **Step 3: `TweaksPanel.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 25 | `tweaks` | `{t.tweaks.title}` |
| 30 | `Close tweaks` (aria) | `{t.a11y.closeTweaks}` |
| 39 | `accent` | `{t.tweaks.accent}` |
| 62 | `density` | `{t.tweaks.density}` |
| 65 | `comfortable` | `{t.tweaks.comfortable}` |
| 65 | `compact` | `{t.tweaks.compact}` |
| 85 | `noise` | `{t.tweaks.noise}` |
| 103 | `Open tweaks panel` (aria) | `{t.a11y.openTweaks}` |

> 47행 `accent: {a}` 동적 aria는 영어 유지(YAGNI).

- [ ] **Step 4: `ProfileShell.tsx`** — `<CommandPalette ... t={t} />`, `<StatusBar ... t={t} />`, `<TweaksPanel t={t} />` 전달 추가.

- [ ] **Step 5: 검사 + 커밋**

Run: `npm run check`
Expected: 통과.
```bash
git add components/profile/CommandPalette.tsx components/profile/StatusBar.tsx components/profile/TweaksPanel.tsx components/profile/ProfileShell.tsx
git commit -m "feat: CommandPalette/StatusBar/Tweaks 다국어 문자열 적용"
```

---

## Task 13: StatsRow 문자열 치환

**Files:**
- Modify: `components/profile/StatsRow.tsx`
- Modify: 호출부(`Hero.tsx` 또는 `RightRailContent` 등 StatsRow를 렌더하는 컴포넌트 — `grep -rn "StatsRow" components/`로 확인)

- [ ] **Step 1: 호출부 확인**

Run: `grep -rn "StatsRow" components/`
Expected: `StatsRow`를 import·렌더하는 파일 식별. 그 부모가 `t`를 이미 받는지 확인하고, 안 받으면 `t: Dict`를 추가해 ProfileShell까지 배선을 연결한다.

- [ ] **Step 2: `StatsRow.tsx`** — `t: Dict` 추가, 아래 치환.

| 행 | old | new |
|---|---|---|
| 25 | `services up` | `{t.stats.servicesUp}` |
| 70 | `blog posts` | `{t.stats.blogPosts}` |

> 39행 `commits · 26w`, 47행 `uptime · 90d`는 코드풍 라벨이라 그대로 둔다(RightRail의 동일 문구와 정책 일치).

- [ ] **Step 3: 배선 + 검사 + 커밋**

Run: `npm run check`
Expected: 통과.
```bash
git add components/profile/StatsRow.tsx <호출부 파일들>
git commit -m "feat: StatsRow 다국어 문자열 적용"
```

---

## Task 14: 포트폴리오 콘텐츠 영어 카피 작성

**Files:**
- Modify: `contents/website/markora/index.md`
- Modify: `contents/website/status/index.md`
- Modify: `contents/website/it-blog/index.md`
- Modify: `contents/website/ai-chatbot/index.md`
- Modify: `contents/website/investment-blog/index.md`
- Modify: `contents/website/inspire-me/index.md`

각 파일의 번역 대상 필드를 `_en`/`_ko` 접미사로 분리한다. 기존 `description`(영어)·`dek`/`overview`(한국어)를 다음 규칙으로 옮긴다:

- `description`(영어) → `description_en`에 그대로, `description_ko`에 한국어 번역 추가
- `dek`(한국어) → `dek_ko`에 그대로, `dek_en`에 영어 번역 추가
- `overview`(한국어) → `overview_ko`에 그대로, `overview_en`에 영어 번역 추가
- `role: Solo` → `role_en: Solo`, `role_ko: 단독`
- `year: 2026 — now` → `year_en: 2026 — now`, `year_ko: 2026 — 현재`
- `status: live` → `status_en: live`, `status_ko: 운영 중`
- 무접미사 필드(`site`, `cover`, `stack`, `order`, `title`, `ext`)는 그대로 유지

- [ ] **Step 1: `markora/index.md` 변환** (예시 — 나머지 파일도 동일 규칙)

기존 frontmatter를 아래 형태로 바꾼다(영어 카피는 한국어 원문 기반으로 직접 작성):

```yaml
---
site: https://markora.advenoh.pe.kr/
title: Markora
cover: cover.png
stack:
  - Kotlin
  - JetBrains Plugin SDK
  - JCEF
  - BlockNote
  - KaTeX
  - Mermaid
ext: .kt
order: 6
status_en: live
status_ko: 운영 중
year_en: 2026 — now
year_ko: 2026 — 현재
role_en: Solo
role_ko: 단독
description_en: A Typora-style WYSIWYG markdown editor plugin for JetBrains IDEs, providing real-time rendering without a separate preview pane.
description_ko: 별도 프리뷰 패널 없이 실시간 렌더링을 제공하는 JetBrains IDE용 Typora 스타일 WYSIWYG 마크다운 에디터 플러그인.
dek_en: A Typora-style WYSIWYG markdown editor plugin for JetBrains IDEs.
dek_ko: JetBrains IDE를 위한 Typora 스타일 WYSIWYG 마크다운 에디터 플러그인.
overview_ko: |
  **왜 만들었나.** JetBrains IDE에서 마크다운을 편집할 때 프리뷰 패널을 오가는 흐름을 없애고, Typora처럼 입력 즉시 렌더링되는 WYSIWYG 경험을 IDE 안으로 가져오고 싶었다.

  - JCEF + BlockNote 기반의 in-place WYSIWYG 에디터
  - `/` 슬래시 명령으로 제목·목록·코드·표·이미지 빠르게 삽입
  - KaTeX 수학 수식 / Mermaid 다이어그램 렌더링
  - IDE 다크/라이트 테마 자동 동기화, 1초 debounced 자동 저장
  - JetBrains IDE 2024.2+ 호환, MIT 오픈소스

  **배운 것.** WYSIWYG 편집기는 키맵보다 'undo가 직관적인가'가 사용자 경험을 좌우한다.
overview_en: |
  **Why I built it.** I wanted to remove the back-and-forth with the preview pane when editing Markdown in JetBrains IDEs, and bring a Typora-style render-as-you-type WYSIWYG experience inside the IDE.

  - In-place WYSIWYG editor built on JCEF + BlockNote
  - `/` slash commands to quickly insert headings, lists, code, tables, images
  - KaTeX math and Mermaid diagram rendering
  - Auto-syncs with IDE dark/light theme, 1s debounced autosave
  - Compatible with JetBrains IDE 2024.2+, MIT open source

  **What I learned.** For a WYSIWYG editor, "is undo intuitive?" shapes the user experience more than the keymap does.
---
```

- [ ] **Step 2: 나머지 5개 파일 동일 변환**

`status`, `it-blog`, `ai-chatbot`, `investment-blog`, `inspire-me` 각 `index.md`에 대해 Step 1과 동일한 규칙을 적용한다. 각 파일의 기존 한국어 `dek`/`overview`를 `_ko`로, 새 영어 번역을 `_en`으로 작성한다.

- [ ] **Step 3: 인코딩 확인**

Run: `file -I contents/website/*/index.md`
Expected: 모두 `charset=utf-8`.

- [ ] **Step 4: 빌드 검증**

Run: `npm run build`
Expected: zod 검증·빌드 통과, `out/index.html`/`out/ko/index.html` 생성.

- [ ] **Step 5: Commit**

```bash
git add contents/website/
git commit -m "feat: 포트폴리오 항목 en/ko 카피 분리"
```

---

## Task 15: 프로필 readme 영어 카피 작성

**Files:**
- Modify: `contents/profile/readme.md`

- [ ] **Step 1: frontmatter + 본문을 en/ko 분리**

`role`/`focus`/`based`/`xp`/`headline`을 `_en`/`_ko`로 분리하고, 본문은 `body_en`/`body_ko`로 옮긴다. 무접미사 배열(`stack`, `db`, `cloud`, `cicd`)은 유지. 예:

```yaml
---
headline_en: Software Engineer
headline_ko: 소프트웨어 엔지니어
role_en: Backend / AI Engineer
role_ko: 백엔드 / AI 엔지니어
focus_en: Distributed systems, Backend, DevOps
focus_ko: 분산 시스템, 백엔드, DevOps
based_en: Seoul, South Korea · open to remote
based_ko: 대한민국 서울 · 원격 근무 가능
xp_en: 10+ years
xp_ko: 10년 이상
stack:
  - Go
  - Python
  - Claude
  - Cursor
  - Java
  - Spring Boot
db:
  - MySQL
  - Redis
  - Kafka
  - Memcached
cloud:
  - Kubernetes
  - Docker
  - Helm
cicd:
  - GitHub Actions
  - ArgoCD
  - Jenkins
body_ko: |
  backend·AI 시스템을 설계하고 운영한다. RAG 파이프라인, 분산 시스템,
  쿠버네티스 GitOps 인프라에 관심이 있으며, 개인 블로그와 서비스를 …(기존 본문 전체)
body_en: |
  I design and operate backend and AI systems. I'm interested in RAG pipelines,
  distributed systems, and Kubernetes GitOps infrastructure, and I run personal blogs and services …(영어 번역)
---
```

> 기존 마크다운 본문(frontmatter 아래 텍스트)은 `body_ko`로 옮긴 뒤 제거하거나, 안전하게 남겨두어도 로더가 `body_ko` 우선이라 무방하다. 깔끔하게 `body_ko`로 통일 권장.

- [ ] **Step 2: 인코딩 확인 + 빌드**

Run: `file -I contents/profile/readme.md && npm run build`
Expected: `charset=utf-8`, 빌드 통과. `/`엔 영어 프로필, `/ko/`엔 한국어 프로필 노출(다음 Task에서 E2E로 확정).

- [ ] **Step 3: Commit**

```bash
git add contents/profile/readme.md
git commit -m "feat: 프로필 readme en/ko 카피 분리"
```

---

## Task 16: 사이트맵 + E2E 스모크 테스트

**Files:**
- Modify: `app/sitemap.ts`
- Create: `tests/i18n.spec.ts`

- [ ] **Step 1: `app/sitemap.ts`에 ko URL + hreflang 추가**

```ts
import type { MetadataRoute } from 'next'
import { siteConfig } from '@/lib/site-config'

export const dynamic = 'force-static'

export default function sitemap(): MetadataRoute.Sitemap {
  const now = new Date()
  return [
    {
      url: `${siteConfig.url}/`,
      lastModified: now,
      changeFrequency: 'monthly',
      priority: 1.0,
      alternates: {
        languages: {
          en: `${siteConfig.url}/`,
          ko: `${siteConfig.url}/ko/`,
        },
      },
    },
    {
      url: `${siteConfig.url}/ko/`,
      lastModified: now,
      changeFrequency: 'monthly',
      priority: 0.9,
    },
  ]
}
```

> 기존 `app/sitemap.ts`에 다른 URL(블로그/포트폴리오 외 경로)이 있으면 보존하고 위 두 항목을 추가/병합한다. 먼저 `cat app/sitemap.ts`로 현 내용을 확인할 것.

- [ ] **Step 2: 실패하는 E2E 테스트 작성 `tests/i18n.spec.ts`**

```ts
import { test, expect } from '@playwright/test'

test('영어 루트는 영어 라벨을 노출한다', async ({ page }) => {
  await page.goto('/')
  await expect(page.getByText('Search & jump')).toBeVisible()
  await expect(page.locator('html')).toHaveAttribute('lang', 'en')
})

test('/ko 는 한국어 라벨을 노출한다', async ({ page }) => {
  await page.goto('/ko/')
  await expect(page.getByText('검색 & 이동')).toBeVisible()
  await expect(page.locator('html')).toHaveAttribute('lang', 'ko')
})

test('언어 토글로 /ko 로 이동한다', async ({ page }) => {
  await page.goto('/')
  await page.getByRole('group', { name: /Switch language|언어 전환/ }).getByText('KO').click()
  await expect(page).toHaveURL(/\/ko\/?$/)
  await expect(page.getByText('검색 & 이동')).toBeVisible()
})
```

- [ ] **Step 3: 테스트 실행해 실패 확인(미구현 항목 있으면)**

Run: `npm run test -- tests/i18n.spec.ts`
Expected: Task 1~15가 끝났다면 통과. 실패하면 해당 라벨 치환/배선 누락을 추적해 수정.

- [ ] **Step 4: 전체 빌드·타입·테스트 그린 확인**

Run: `npm run check && npm run build && npm run test`
Expected: 모두 통과, `out/index.html`·`out/ko/index.html` 생성.

- [ ] **Step 5: Commit**

```bash
git add app/sitemap.ts tests/i18n.spec.ts
git commit -m "feat: 다국어 사이트맵 hreflang 및 E2E 스모크 테스트"
```

---

## Self-Review 메모

- **Spec 커버리지:** 라우팅(Task 6) / 사전(Task 1) / 콘텐츠 필드 접미사(Task 2,3,14,15) / 토글·자동전환(Task 7) / SEO hreflang·sitemap·structured-data(Task 4,6,16) / 테스트(Task 16) — spec의 모든 섹션이 태스크로 매핑됨.
- **터미널 토큰 정책:** 코드풍 토큰/기호는 번역하지 않는다는 정책을 각 치환표에 명시해 과번역을 방지.
- **타입 안전망:** `ko: Dict`가 키 누락을 컴파일 타임에 강제(Task 1 Step 4). 컴포넌트 배선 누락은 `npm run check`로 검출.
- **알려진 위험:** Task 13 `StatsRow` 호출부는 코드에서 확인 후 배선(Step 1에 grep 명시). Task 16 sitemap은 기존 내용 병합 주의.
